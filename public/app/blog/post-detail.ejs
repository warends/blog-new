<div class="container">
  <div class="row">
    <div class="col-xs-12 post-detials">
      <h1>{{post.title}}</h1>
      <h6>{{post.postedDate | date}}</h6>
      <h5 class="categories" ng-repeat="cat in post.categories">{{cat}},</h5>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-8">
      <p>{{post.body}}</p>
      <p class="small">{{post.author}}</p>
    </div>
  </div>
</div>
